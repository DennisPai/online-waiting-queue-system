# 生產環境 Dockerfile for Backend
FROM node:18-alpine

# 建立應用目錄
WORKDIR /app

# 複製 package.json 和 package-lock.json
COPY package*.json ./

# 安裝依賴（僅生產環境）
RUN npm ci --only=production

# 複製應用程式碼
COPY . .

# 建立非 root 使用者
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nodejs -u 1001

# 變更檔案擁有者
USER nodejs

# 暴露端口
EXPOSE 8080

# 啟動應用
CMD ["npm", "start"] 