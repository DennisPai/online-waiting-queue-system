# 伺服器設定
PORT=8080
NODE_ENV=production

# MongoDB 連接 (Zeabur將自動提供，無需手動設定)
# MONGODB_URI=mongodb://username:password@host:port/database

# JWT 設定 (⚠️ 必須使用強密碼，至少32個字符)
JWT_SECRET=your_very_secure_jwt_secret_key_at_least_32_characters_long_please_change_this
JWT_EXPIRES_IN=1d

# CORS 設定 (設定為前端域名)
CORS_ORIGIN=https://your-frontend-domain.zeabur.app
SOCKET_CORS_ORIGIN=https://your-frontend-domain.zeabur.app

# Zeabur 部署重要說明
# 1. JWT_SECRET 必須設定為安全的隨機字符串（至少32字符）
# 2. CORS_ORIGIN 和 SOCKET_CORS_ORIGIN 設定為前端的實際域名
# 3. MONGODB_URI 由Zeabur的MongoDB服務自動提供，無需手動設定
# 4. 部署順序：MongoDB → 後端 → 前端 → 配置CORS 