# 伺服器設定
PORT=8080
NODE_ENV=production

# MongoDB 連接 (Zeabur將自動提供)
MONGODB_URI=mongodb://admin:password@mongodb:27017/queue_system?authSource=admin

# JWT 設定 (請在Zeabur中設定強密碼)
JWT_SECRET=your_production_jwt_secret_key_please_change_this
JWT_EXPIRES_IN=1d

# CORS 設定 (Zeabur部署後會自動更新)
CORS_ORIGIN=https://your-app-domain.zeabur.app
SOCKET_CORS_ORIGIN=https://your-app-domain.zeabur.app

# Zeabur 部署說明
# 1. JWT_SECRET 必須設定為安全的隨機字符串
# 2. CORS_ORIGIN 設定為您的實際域名
# 3. MONGODB_URI 通常由Zeabur的MongoDB服務自動提供 